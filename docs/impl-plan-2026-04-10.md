# doxus TDD 구현 계획 — 2026-04-10

> **기준 커밋:** `cd379e4`  
> **작성:** Analyst + Architect 에이전트 공동 분석  
> **방식:** TDD (Red → Green → Refactor)

---

## 의존성 그래프

```
A-1: P0-1 (PluginManager::get_source)
  └─► A-2: P1-4 (plugin install wiring)
  └─► A-3: P1-1 (Confluence fetch_all fix)

B-1: P2-4 (find_path cycle fix)      ─► 독립 (병렬 가능)
B-2: P1-6 (CLI 누락 명령어)           ─► A-1 이후
B-3: P2-1 (MCP migration V9)         ─► 독립 (병렬 가능)
C-1: P2-2 (protocol dedup)           ─► 독립 (병렬 가능)
```

---

## 작업 그룹

### 그룹 A: P0 블로커 (순차)

#### A-1: P0-1 — `PluginManager::get_source` Confluence/GitHub arm 추가
- **파일:** `crates/core/src/plugin/manager.rs:78-84`
- **현재 상태:** `"com.doxus.obsidian"` 만 반환, 나머지 `None`
- **예상 LOC:** ~30

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/core/tests/plugin_manager_source.rs` |
| | `test_get_source_returns_confluence_plugin()` |
| | `test_get_source_returns_github_plugin()` |
| | `test_get_source_unknown_returns_none()` |
| | `test_get_source_metadata_id_matches()` |
| **Green** | `manager.rs:79`에 Confluence/GitHub arm 추가 |
| **Refactor** | registry lookup 패턴 가능성 검토 |

- [ ] 완료

#### A-2: P1-4 — plugin install에 실제 WASM 다운로드 + PluginInstaller 연결
- **파일 (CLI):** `crates/cli/src/main.rs:357-363` — DB INSERT만
- **파일 (MCP):** `crates/mcp-server/src/lib.rs:1370-1387` — DB INSERT만
- **파일 (Installer):** `crates/core/src/marketplace/installer.rs:38-55` — 구현됨, 미연결
- **예상 LOC:** ~80

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/core/tests/plugin_install_integration.rs` |
| | `test_install_downloads_and_writes_wasm()` |
| | `test_install_rejects_checksum_mismatch()` |
| | `test_mcp_plugin_install_calls_installer()` |
| | `test_cli_plugin_install_calls_installer()` |
| **Green** | MCP/CLI에서 `PluginInstaller::install()` 호출 연결 |
| **Refactor** | 공통 `install_plugin()` 함수를 core에 추출 |

- [ ] 완료

#### A-3: P1-1 — Confluence `fetch_all` `expand=body.storage` + OAuth 토큰 분기
- **파일:** `crates/plugins/confluence/src/lib.rs:352-362`
- **현재 상태:** `expand` 파라미터 없음 → body 항상 빈 값; Bearer 토큰 고정
- **예상 LOC:** ~25

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/plugins/confluence/tests/fetch_all_expand.rs` |
| | `test_fetch_all_sends_expand_body_storage()` |
| | `test_fetch_all_returns_page_content()` |
| | `test_fetch_all_uses_oauth_token_when_available()` |
| | `test_fetch_all_falls_back_to_api_token()` |
| **Green** | `expand=body.storage` 추가, OAuth 분기 추가 |
| **Refactor** | `fn auth_header(&self) -> String` 헬퍼 추출 |

- [ ] 완료

---

### 그룹 B: 병렬 가능 항목

#### B-1: P2-4 — `tool_find_path` 사이클 체크 개선
- **파일:** `crates/mcp-server/src/lib.rs:1137`
- **문제:** `NOT LIKE '%' || id || '%'` 방식 → `doc-1` vs `doc-10` false positive
- **예상 LOC:** ~30

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/mcp-server/tests/find_path_cycle.rs` |
| | `test_find_path_no_false_positive_on_substring_ids()` |
| | `test_find_path_detects_real_cycle()` |
| | `test_find_path_handles_max_hops()` |
| **Green** | 구분자(`->`) 기반 LIKE 패턴으로 변경 |
| **Refactor** | 대규모 그래프 성능 노트 주석 추가 |

- [ ] 완료

#### B-2: P1-6 — CLI 누락 명령어 추가
- **파일:** `crates/cli/src/main.rs:13-36, 399-411`
- **누락:** `Sync`, `Agent` 서브커맨드, `WorkspaceAction::Delete`
- **예상 LOC:** ~100

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/cli/tests/cli_commands.rs` |
| | `test_parse_workspace_delete()` |
| | `test_parse_sync_command()` |
| | `test_parse_agent_start()` |
| | `test_workspace_delete_removes_from_db()` |
| | `test_sync_invokes_sync_runner()` |
| **Green** | enum + 핸들러 추가 |
| **Refactor** | 핸들러를 `handlers/` 모듈로 분리 검토 |

- [ ] 완료

#### B-3: P2-1 — Migration V9 (`document_aliases`, `chunks` 테이블)
- **파일:** `crates/core/src/db/migrations/` (신규), `lib.rs:677-701, 781-796`
- **문제:** `resolve_alias`·`inspect_document` 가 존재하지 않는 테이블 쿼리
- **예상 LOC:** ~40

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/core/tests/migration_v9.rs` |
| | `test_document_aliases_table_exists_after_migration()` |
| | `test_chunks_table_exists_after_migration()` |
| | `test_resolve_alias_returns_document()` |
| | `test_inspect_document_returns_chunk_count()` |
| **Green** | `V9__add_document_aliases_and_chunks.sql` 작성 + 마이그레이션 runner 등록 |
| **Refactor** | 없음 |

- [ ] 완료

---

### 그룹 C: Design Debt

#### C-1: P2-2 — SidecarMessage/HostMessage 중복 제거
- **파일:** `crates/agent/src/sidecar.rs:24-29`, `crates/agent/src/protocol.rs:8-13`
- **문제:** 동일 4개 variant 중복 정의
- **예상 LOC:** ~20 (삭제 위주)

| TDD 단계 | 내용 |
|-----------|------|
| **Red** | `crates/agent/tests/protocol_unification.rs` |
| | `test_host_message_serializes_correctly()` |
| | `test_sidecar_send_accepts_host_message()` |
| **Green** | `SidecarMessage` 제거, `HostMessage`로 통일 |
| **Refactor** | `protocol.rs` 단일 진실 원천 주석 추가 |

- [ ] 완료

---

## 실행 순서

```
Phase 1 (순차):  A-1 → A-2 → A-3
Phase 2 (병렬):  B-1 | B-3 | C-1  (A와 무관, 즉시 시작 가능)
                 B-2             (A-1 완료 후)
```

---

## 진행 로그

| 날짜 | 항목 | 상태 | 비고 |
|------|------|------|------|
| 2026-04-10 | 계획 수립 (Analyst + Architect) | ✅ | |
| 2026-04-10 | A-1: PluginManager get_source | ⬜ | |
| 2026-04-10 | A-2: plugin install wiring | ⬜ | |
| 2026-04-10 | A-3: Confluence expand fix | ⬜ | |
| 2026-04-10 | B-1: find_path cycle fix | ⬜ | |
| 2026-04-10 | B-2: CLI 누락 명령어 | ⬜ | |
| 2026-04-10 | B-3: Migration V9 | ⬜ | |
| 2026-04-10 | C-1: protocol dedup | ⬜ | |

---

## Analyst 오진 수정 사항

| 항목 | 최초 주장 | 실제 코드 |
|------|----------|-----------|
| P2-1 `doxus_plugin_info` | "선언만 있음" | `lib.rs:1563-1579` 완전 구현 |
| P2-1 `doxus_resolve_alias` | "로직 없음" | `lib.rs:677-701` SQL 구현됨; 마이그레이션만 누락 |
| P1-1 OAuth 분기 | "전혀 없음" | `oauth_start`/`oauth_exchange` 구현됨; `fetch_all` 헤더 분기만 누락 |
| P2-3 DueInstance NULL | "미완료" | `sync/db.rs:58` `unwrap_or_default` 적용 완료 |

---

## 참고 링크

- [`docs/todo.md`](todo.md) — 우선순위별 작업 목록
- [`UNIMPLEMENTED_ITEMS.md`](../UNIMPLEMENTED_ITEMS.md) — 전체 분석
- [`.claude/rules/architecture.md`](../.claude/rules/architecture.md) — 아키텍처 원칙
